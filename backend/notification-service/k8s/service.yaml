apiVersion: v1
kind: Service
metadata:
  name: notification-service-service-deployment
spec:
  type: NodePort
  selector:
    app: notification-service
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 32000