apiVersion: v1
kind: Service
metadata:
  name: communication-service
spec:
  type: ClusterIP
  selector:
    app: communication-service
  ports:
  - port: 8080        # internal cluster port (can stay 8080)
    targetPort: 5000  # MUST match container port