apiVersion: v1
kind: Service
metadata:
  name: frontend-app-service
spec:
  type: NodePort   # 👈 IMPORTANT (to access from browser)
  selector:
    app: frontend-app
  ports:
  - port: 5173
    targetPort: 5173
    nodePort: 30007